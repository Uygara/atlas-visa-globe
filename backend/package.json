{
  "name": "atlas-visa-scraper",
  "version": "1.0.0",
  "description": "Daily refresh of visa policy data from Wikipedia",
  "main": "scraper.js",
  "scripts": {
    "scrape": "node scraper.js --all",
    "scrape:dry": "node scraper.js --all --dry-run",
    "test": "node scraper.js --passport \"United States\" --dry-run"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
