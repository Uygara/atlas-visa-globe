# generated / downloaded — rebuilt by `npm run www`
node_modules/
www/
.cache/
# GoogleService-Info.plist and google-services.json ARE committed: they hold public project
# identifiers (like the web config), and a Mac build needs them. Access is enforced by
# firestore.rules and the sign-in provider settings, not by hiding these.
