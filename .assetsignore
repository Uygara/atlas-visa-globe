# Not part of the website: internal notes, the Capacitor shell, editor config.
# Cloudflare deploys the repo root as static assets, so anything listed here stays private.
app/
*.md
docs/
.claude/
.secrets/
store/
